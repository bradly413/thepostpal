# Plan — Thu Jun 4, 2026

## Status at end of Jun 3
✅ **Production is LIVE and verified** at posterboysocial.com — multi-tenant RLS dashboard, plan-tier adaptive (Solo/Command). 9/9 API smoke checks pass. Neon prod DB migrated + RLS enforced. `DATABASE_URL` set on Vercel. All work merged to `main`.

What's NOT yet enabled (degrades gracefully — nothing is broken, these features are just off):
Stripe billing · S3 durable uploads · Upstash durable signups · Leonardo Studio HD · `NEXT_PUBLIC_APP_URL`.

---

## P0 — Real-world validation (do first, ~45 min)
The API smoke passed, but nothing's been clicked through the actual UI in prod yet.
- [ ] Log into posterboysocial.com as **demo/demo123** → confirm the **streamlined Solo surface** (no location switcher, no approval pipeline, composer = Schedule/Publish), no layout gaps.
- [ ] **Sign up a fresh account** → confirm onboarding ("Brand Architect") completes and lands in the dashboard with its own location; confirm it canNOT see demo-org data.
- [ ] Create a post + calendar event + upload a photo through the UI → confirm they persist on reload.
- [ ] Temporarily flip the demo org to `house_account` in Neon → reload → confirm the **full Command surface** appears (switcher, roll-up, approval) → flip back to `solo`.

## P1 — Make signups + uploads durable (beta-critical, ~30 min)
Right now signups and uploads use ephemeral storage that's lost on redeploy.
- [ ] **Upstash** — add `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` on Vercel (or alias the existing `KV_REST_API_URL/_TOKEN` values). Without this, beta tester accounts vanish on the next deploy.
- [ ] **S3** — create a bucket (or Cloudflare R2) and set `S3_BUCKET`, `S3_REGION`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY` (+ `S3_ENDPOINT`/`S3_PUBLIC_BASE_URL` for R2). Without this, uploaded photos disappear on redeploy.
- [ ] Redeploy, re-run `./scripts/smoke-prod.sh`.

## P1 — Set canonical URL (~2 min)
- [ ] `NEXT_PUBLIC_APP_URL = https://www.posterboysocial.com` on Vercel (fixes OAuth redirect base / canonical links).

## P2 — Billing (only if charging during beta)
- [ ] Decide: do friendly beta testers pay? If **no**, skip Stripe for now (Solo/Command checkout just stays off, app works fine).
- [ ] If **yes**: set `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, and the 4 price IDs (`STRIPE_PRICE_SOLO_MONTHLY/ANNUAL`, `STRIPE_PRICE_COMMAND_BASE_MONTHLY/LOCATION_MONTHLY`); point a Stripe webhook at `/api/webhooks/stripe`; test a checkout.

## P2 — Housekeeping
- [ ] **Rotate the Neon DB password** (it was pasted into chat yesterday to run the migration). Neon → role → reset password → update `DATABASE_URL` on Vercel with the new pooled string → redeploy.
- [ ] **Delete the orphan `thepostpal-readable-v2`** Vercel project (it's the red ✗ on every PR; also has a stale `ANTHROPIC_API_KEY`).
- [ ] Leonardo: add `LEONARDO_API_KEY` if you want Studio HD upscale/remove-bg working.
- [ ] Note: any demo sessions from before yesterday need a **re-login** to get DB provisioning.

## P3 — Beta launch prep
- [ ] Refresh `docs/BETA-TESTER-INSTRUCTIONS.md` for the live URL + signup flow.
- [ ] Send to the friendly testers.

---

### Handy commands
- Smoke test prod: `./scripts/smoke-prod.sh`
- Re-run DB migration (new prod DB): `./scripts/deploy-prod-db.sh "<direct-url>"`
- Watch deploys: `npx vercel ls angie-social-portal --scope bradly413s-projects`
- Env checklist reference: `docs/PROD-ENV-CHECKLIST.md` · DB setup: `docs/PROD-DB-SETUP.md`

### Suggested order
P0 validation → P1 Upstash + S3 + APP_URL → redeploy + smoke → P2 password rotation + orphan cleanup → decide on Stripe → beta invites.
